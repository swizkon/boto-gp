<h1>The tracks available</h1>

<hr />

<%= for circuit <- @circuits do %>
<div class="jumbotron circuit-preview">
  <h1><%= circuit.name %></h1>
  <canvas class="circuit-preview" 
    id="circuit-preview-<%= circuit.id %>" 
    width="600" height="400"
    data-checkpoints="<%= circuit.checkpoints %>"></canvas>

  <%= link "New race on this circuit", to: endurance_path(@conn, :show, circuit), class: "btn btn-primary btn-lg" %>
  <%= link "Practice", to: endurance_path(@conn, :practice, circuit), class: "btn btn-default" %>
<%= link "Modify", to: endurance_path(@conn, :edit, circuit), class: "btn btn-default" %>
</div>

<% end %>

<hr />
<%= link "New circuit", to: endurance_path(@conn, :new) %>
