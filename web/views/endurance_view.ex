defmodule BotoGP.EnduranceView do
  use BotoGP.Web, :view
  import BotoGP.Router.Helpers


  #def render("index.html", %{circuits: circuits}) do
    #circuits
    #Enum.map(circuits, &Map.take(&1, [:id, :name]))
  #end
  
end
